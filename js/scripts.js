var a = prompt('Wpisz wartość a'),
    b = prompt('Wpisz wartość b'),
    value = (a * a) + (2 * a * b) - (b * b);
if (value > 0)
    alert('wynik dodatni');
else if (value < 0)
    alert('wynik ujemny');
console.log(' (a * a) + (2 * a * b) - (b * b)= ' + value);git status
